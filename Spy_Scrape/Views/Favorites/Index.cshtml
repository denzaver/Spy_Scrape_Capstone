@model IEnumerable<Spy_Scrape.Models.Favorite>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>My Saved Ads</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Customer)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FavoriteId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FavoriteId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FavoriteId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FavoriteId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
