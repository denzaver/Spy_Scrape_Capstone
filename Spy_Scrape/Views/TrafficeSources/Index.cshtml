@model IEnumerable<Spy_Scrape.Models.TrafficeSource>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TrafficSourceName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TrafficSourceName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TrafficSourceId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TrafficSourceId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TrafficSourceId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
